(function(window, angular, undefined) {
  'use strict';

  angular
    .module('ngKanbanBoard', [
      'ngKanbanBoard.controllers',
      'ngKanbanBoard.directives'
    ]);

})(window, window.angular);
